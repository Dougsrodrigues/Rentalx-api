import { inject, injectable } from 'tsyringe';

// @injectable()
class UploadCarImageUseCase {
  // constructor(
  // @inject('NameRepository')
  // private NameRepository: INameRepository
  // ) {}
  async execute(): Promise<void> {}
}

export { UploadCarImageUseCase };
